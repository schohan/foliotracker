"""Model name constants for ADK agents."""

from app.configs.settings import settings

DEFAULT_MODEL = settings.default_model
FAST_MODEL = settings.default_model
REASONING_MODEL = settings.default_model
